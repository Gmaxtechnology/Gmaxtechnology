(()=>{const cfg=window.GMAX_CONFIG||{},$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],fmt=n=>'₦'+Number(n||0).toLocaleString('en-NG');let client=null,editing=null,cache=[];const configured=()=>!!(cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY);function note(t,ok=true){const e=$('.toast');e.textContent=t;e.style.background=ok?'#071b33':'#a52f36';e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2500)}function tab(n){$$('[data-tab]').forEach(b=>b.classList.toggle('active',b.dataset.tab===n));$$('.panel').forEach(p=>p.classList.toggle('active',p.id==='panel-'+n))}async function init(){if(!configured()){ $('#setup').style.display='block';return}client=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);const{data}=await client.auth.getSession();if(data.session)show();else $('#login').style.display='block'}$('#login-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),{data,error}=await client.auth.signInWithPassword({email:f.get('email'),password:f.get('password')});if(error)return $('#login-msg').innerHTML='<div class="notice bad">'+error.message+'</div>';const{data:a}=await client.from('admin_users').select('user_id').eq('user_id',data.user.id).maybeSingle();if(!a){await client.auth.signOut();return $('#login-msg').innerHTML='<div class="notice bad">This account is not registered as a GMAX administrator.</div>'}show()};async function show(){$('#login').style.display='none';$('#dash').style.display='grid';await Promise.all([loadProducts(),loadOrders(),loadMessages()])}$$('[data-tab]').forEach(b=>b.onclick=()=>tab(b.dataset.tab));$('#new-product').onclick=()=>{editing=null;$('#product-form').reset();tab('form')};$('#logout').onclick=async()=>{if(client)await client.auth.signOut();location.reload()};async function loadProducts(){const{data,error}=await client.from('products').select('*').order('created_at',{ascending:false});if(error)return note(error.message,false);cache=data||[];$('#products-body').innerHTML=cache.map(p=>`<tr><td><b>${p.name}</b><br><small>${p.category||''}</small></td><td>${fmt(p.price)}</td><td>${p.in_stock?'In stock':'Out'}</td><td><button class="mini edit" data-edit="${p.id}">Edit</button> <button class="mini delete" data-del="${p.id}">Delete</button></td></tr>`).join('');$$('[data-edit]').forEach(b=>b.onclick=()=>edit(b.dataset.edit));$$('[data-del]').forEach(b=>b.onclick=()=>del(b.dataset.del))}function edit(id){const p=cache.find(x=>x.id===id);if(!p)return;editing=id;const f=$('#product-form');['name','category','brand','price','old_price','description','stock_quantity'].forEach(k=>f.elements[k].value=p[k]??'');f.elements.existing_image.value=p.image_url||'';f.elements.featured.checked=!!p.featured;f.elements.in_stock.checked=!!p.in_stock;f.elements.specs.value=Object.entries(p.specs||{}).map(([k,v])=>`${k}: ${v}`).join('\n');tab('form')}async function del(id){if(!confirm('Delete this product?'))return;const{error}=await client.from('products').delete().eq('id',id);if(error)return note(error.message,false);loadProducts();note('Product deleted')}$('#product-form').onsubmit=async e=>{e.preventDefault();const f=e.target,fd=new FormData(f);let image_url=fd.get('existing_image')||'',file=fd.get('image');if(file&&file.size){const path=`products/${Date.now()}-${file.name.replace(/[^a-z0-9._-]/gi,'-')}`,{error}=await client.storage.from('product-images').upload(path,file);if(error)return note('Image upload failed: '+error.message,false);image_url=client.storage.from('product-images').getPublicUrl(path).data.publicUrl}let specs={};String(fd.get('specs')||'').split('\n').filter(Boolean).forEach(line=>{const i=line.indexOf(':');if(i>0)specs[line.slice(0,i).trim()]=line.slice(i+1).trim()});const o={name:fd.get('name'),category:fd.get('category'),brand:fd.get('brand'),price:Number(fd.get('price')),old_price:fd.get('old_price')?Number(fd.get('old_price')):null,stock_quantity:Number(fd.get('stock_quantity')||0),description:fd.get('description'),image_url,specs,featured:fd.get('featured')==='on',in_stock:fd.get('in_stock')==='on',active:true};const r=editing?await client.from('products').update(o).eq('id',editing):await client.from('products').insert(o);if(r.error)return note(r.error.message,false);editing=null;f.reset();await loadProducts();tab('products');note('Product saved')};async function loadOrders(){const{data}=await client.from('orders').select('*').order('created_at',{ascending:false}).limit(100);$('#orders-body').innerHTML=(data||[]).map(o=>`<tr><td><b>${o.order_number}</b><br><small>${new Date(o.created_at).toLocaleString()}</small></td><td>${o.customer_name}<br><small>${o.phone}</small></td><td>${fmt(o.total)}</td><td>${o.status}</td></tr>`).join('')}async function loadMessages(){const{data}=await client.from('contact_messages').select('*').order('created_at',{ascending:false}).limit(100);$('#messages-body').innerHTML=(data||[]).map(m=>`<tr><td>${m.name}<br><small>${m.email}</small></td><td><b>${m.subject||''}</b><br>${m.message}</td><td>${new Date(m.created_at).toLocaleString()}</td></tr>`).join('')}document.addEventListener('DOMContentLoaded',init)})();
