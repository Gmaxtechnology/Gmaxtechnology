(()=>{const cfg=window.GMAX_CONFIG||{},seed=window.GMAX_PRODUCTS||[];let products=[...seed],client=null;const $=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];const fmt=n=>'₦'+Number(n||0).toLocaleString('en-NG');const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const configured=()=>!!(cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY);
function toast(t){const e=$('.toast');e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2200)}function cart(){try{return JSON.parse(localStorage.getItem('gmax_cart')||'[]')}catch{return[]}}function save(c){localStorage.setItem('gmax_cart',JSON.stringify(c));count()}function count(){const n=cart().reduce((s,i)=>s+i.qty,0);$$('[data-cart-count]').forEach(e=>e.textContent=n)}function art(p){return p.image_url?`<img src="${esc(p.image_url)}" alt="${esc(p.name)}">`:`<div class="product-emoji">${p.icon||'🔌'}</div>`}function card(p){return `<article class="product"><div class="product-image">${art(p)}${p.demo?'<span class="badge">Demo</span>':p.featured?'<span class="badge">Featured</span>':''}<span class="badge ${p.in_stock===false?'out':'stock'}">${p.in_stock===false?'Out of stock':'In stock'}</span></div><div class="product-body"><div class="product-cat">${esc(p.category)}</div><h3>${esc(p.name)}</h3><div class="price-row"><span class="price">${fmt(p.price)}</span>${p.old_price?`<span class="old">${fmt(p.old_price)}</span>`:''}</div>${p.demo?'<div class="demo">Demo item — replace with current stock before public launch.</div>':''}<div class="product-actions"><button class="add" data-add="${esc(p.id)}">Add to cart</button><button class="view" data-view="${esc(p.id)}">⌕</button></div></div></article>`}
async function load(){if(configured()&&window.supabase){try{client=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);const{data,error}=await client.from('products').select('*').eq('active',true).order('created_at',{ascending:false});if(!error&&data?.length)products=data}catch(e){console.warn(e)}}renderFeatured();setupFilters();renderShop()}
function bind(){$$('[data-add]').forEach(b=>b.onclick=()=>add(b.dataset.add));$$('[data-view]').forEach(b=>b.onclick=()=>view(b.dataset.view))}function add(id){const p=products.find(x=>String(x.id)===String(id));if(!p||p.in_stock===false)return toast('This item is out of stock.');const c=cart(),i=c.find(x=>String(x.id)===String(id));i?i.qty++:c.push({id:p.id,name:p.name,price:Number(p.price),category:p.category,image_url:p.image_url||'',icon:p.icon||'🔌',qty:1});save(c);toast('Added to cart')}
function renderFeatured(){const e=$('#featured-products');let list=products.filter(p=>p.featured).slice(0,8);if(!list.length)list=products.slice(0,8);e.innerHTML=list.map(card).join('');bind()}
function setupFilters(){const cat=$('#filter-category'),brand=$('#filter-brand');[...new Set(products.map(p=>p.category).filter(Boolean))].sort().forEach(v=>cat.insertAdjacentHTML('beforeend',`<option>${esc(v)}</option>`));[...new Set(products.map(p=>p.brand).filter(Boolean))].sort().forEach(v=>brand.insertAdjacentHTML('beforeend',`<option>${esc(v)}</option>`));['#shop-search','#filter-category','#filter-brand','#filter-sort'].forEach(s=>$(s).addEventListener(s==='#shop-search'?'input':'change',renderShop))}
function renderShop(){let list=[...products],q=$('#shop-search').value.toLowerCase().trim(),c=$('#filter-category').value,b=$('#filter-brand').value,s=$('#filter-sort').value;if(q)list=list.filter(p=>`${p.name} ${p.category} ${p.brand||''}`.toLowerCase().includes(q));if(c)list=list.filter(p=>p.category===c);if(b)list=list.filter(p=>p.brand===b);if(s==='low')list.sort((a,b)=>a.price-b.price);if(s==='high')list.sort((a,b)=>b.price-a.price);$('#result-count').textContent=`${list.length} product${list.length===1?'':'s'}`;$('#shop-products').innerHTML=list.length?list.map(card).join(''):'<div class="empty">No products match your search.</div>';bind()}
function view(id){const p=products.find(x=>String(x.id)===String(id));if(!p)return;let specs=p.specs||{};if(typeof specs==='string')try{specs=JSON.parse(specs)}catch{specs={}};$('#product-modal-content').innerHTML=`<div class="product-image" style="height:260px;border-radius:16px">${art(p)}</div><div class="product-cat" style="margin-top:18px">${esc(p.category)}</div><h2 style="color:var(--navy);margin:6px 0">${esc(p.name)}</h2><div class="price-row"><span class="price">${fmt(p.price)}</span>${p.old_price?`<span class="old">${fmt(p.old_price)}</span>`:''}</div><p style="color:var(--muted);line-height:1.7">${esc(p.description||'')}</p><div class="specs">${Object.entries(specs).map(([k,v])=>`<div class="spec"><small>${esc(k)}</small><strong>${esc(v)}</strong></div>`).join('')}</div><button class="btn btn-primary" id="modal-add">Add to cart</button>`;$('#modal-add').onclick=()=>{add(id);$('#product-modal').classList.remove('show')};$('#product-modal').classList.add('show')}
function renderCart(){const c=cart();$('#cart-list').innerHTML=c.length?c.map(i=>`<div class="cart-item"><div class="cart-thumb">${i.image_url?`<img src="${esc(i.image_url)}">`:i.icon}</div><div><strong>${esc(i.name)}</strong><div style="color:var(--blue);font-weight:900;margin-top:4px">${fmt(i.price)}</div><div class="qty"><button data-dec="${esc(i.id)}">−</button><b>${i.qty}</b><button data-inc="${esc(i.id)}">+</button><span class="remove" data-remove="${esc(i.id)}">Remove</span></div></div><strong>${fmt(i.price*i.qty)}</strong></div>`).join(''):'<div class="empty"><h3>Your cart is empty</h3><p>Add products from the store to continue.</p></div>';const total=c.reduce((s,i)=>s+i.price*i.qty,0);$('#cart-subtotal').textContent=fmt(total);$('#cart-total').textContent=fmt(total);$$('[data-inc]').forEach(b=>b.onclick=()=>qty(b.dataset.inc,1));$$('[data-dec]').forEach(b=>b.onclick=()=>qty(b.dataset.dec,-1));$$('[data-remove]').forEach(b=>b.onclick=()=>{save(cart().filter(i=>String(i.id)!==String(b.dataset.remove)));renderCart()})}
function qty(id,d){let c=cart(),i=c.find(x=>String(x.id)===String(id));if(i){i.qty+=d;if(i.qty<=0)c=c.filter(x=>String(x.id)!==String(id));save(c);renderCart()}}
$('#cart-open').onclick=()=>{renderCart();$('#cart-modal').classList.add('show')};$('[data-cart-close]').onclick=()=>$('#cart-modal').classList.remove('show');$('[data-close]').onclick=()=>$('#product-modal').classList.remove('show');$$('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)m.classList.remove('show')}));$('#mobile-menu').onclick=()=>$('#navlinks').classList.toggle('open');
$$('[data-cat-link]').forEach(a=>a.addEventListener('click',()=>{const c=a.dataset.catLink;setTimeout(()=>{$('#filter-category').value=c;renderShop()},50)}));$('#global-search').onsubmit=e=>{e.preventDefault();const q=$('#global-search-input').value;location.hash='shop';$('#shop-search').value=q;renderShop()};
$('#contact-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target),o=Object.fromEntries(f.entries());if(configured()&&window.supabase){try{if(!client)client=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);const{error}=await client.from('contact_messages').insert(o);if(!error){e.target.reset();return toast('Message sent successfully.')}}catch{}}const text=`Website enquiry from ${o.name}\nPhone: ${o.phone||'-'}\nEmail: ${o.email}\nSubject: ${o.subject}\n\n${o.message}`;window.open(`https://wa.me/${cfg.WHATSAPP_NUMBER}?text=${encodeURIComponent(text)}`,'_blank')};
$('#checkout-form').onsubmit=async e=>{e.preventDefault();const c=cart();if(!c.length)return toast('Your cart is empty.');const f=new FormData(e.target),total=c.reduce((s,i)=>s+i.price*i.qty,0),no='GMAX-'+Date.now().toString().slice(-8),order={order_number:no,customer_name:f.get('name'),phone:f.get('phone'),email:f.get('email'),address:f.get('address'),notes:f.get('notes'),items:c,total,status:'new'};if(configured()&&window.supabase){try{if(!client)client=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY);await client.from('orders').insert(order)}catch{}}const lines=c.map(i=>`• ${i.name} × ${i.qty} — ${fmt(i.price*i.qty)}`).join('\n');const text=`Hello GMAX Technology, I want to place an order.\n\nOrder: ${no}\nCustomer: ${order.customer_name}\nPhone: ${order.phone}\nEmail: ${order.email||'-'}\nAddress: ${order.address}\n\n${lines}\n\nTotal: ${fmt(total)}\nNotes: ${order.notes||'-'}`;window.open(`https://wa.me/${cfg.WHATSAPP_NUMBER}?text=${encodeURIComponent(text)}`,'_blank')};count();load()})();
